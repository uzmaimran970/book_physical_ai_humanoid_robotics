# Static Site - No Backend Required
# This Docusaurus site runs as a static site with no backend dependencies.
# UI components are provided as examples only and do not connect to any backend service.
